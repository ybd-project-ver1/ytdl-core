import { InternalDownloadOptions } from '@/core/types';

function _getFullInfo(link: string, options: InternalDownloadOptions) {}

function getFullInfo(link: string, options: InternalDownloadOptions) {}

export { getFullInfo };
